var mongoose = require("mongoose");
var Schema = mongoose.Schema;

var careerpath = new Schema({
		//Title://[
		       //{Path1:[]},
		       //{Path2:[]}
		       //]
})

module.exports = mongoose.model("CareerPath",careerpath);